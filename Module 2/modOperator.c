#include <stdio.h>
// This function calculates the remainder of the division of two integers, a and b, using the modulus operator, and prints the result.

int main()
{
    int a = 12;
    int b = 5;
    int mod = a % b;

    printf("%d", mod);
    return 0;
}